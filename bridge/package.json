{
  "name": "underpixel-bridge",
  "version": "0.1.0",
  "description": "UnderPixel Bridge — MCP server for visual-API correlation",
  "type": "module",
  "bin": {
    "underpixel-bridge": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
        "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "fastify": "^5.3.0",
    "@fastify/cors": "^11.0.0",
    "underpixel-shared": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0",
    "@types/node": "^22.0.0"
  },
  "files": [
    "dist",
    "scripts"
  ],
  "license": "MIT"
}
